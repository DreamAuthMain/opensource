{
  "private": true,
  "name": "root",
  "license": "ISC",
  "type": "module",
  "scripts": {
    "clean": "git clean -fdx -e '.vscode' -e '.env*'",
    "lint": "pnpm -r exec -- eslint src",
    "build": "pnpm -r --stream run build",
    "test": "pnpm lint -- --max-errors=0 && vitest run",
    "modernize": "npx npm-check-updates@latest -i --workspaces --root --format group"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18"
  },
  "engineStrict": true,
  "devDependencies": {
    "@dreamauth/eslint-plugin": "workspace:*",
    "@testing-library/react": "^14.1.2",
    "@types/eslint": "^8.44.9",
    "@types/node": "^20.10.5",
    "@vitest/coverage-v8": "^1.1.0",
    "eslint": "^8.56.0",
    "eslint-config-rational": "^4.0.5",
    "jsdom": "^23.0.1",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0",
    "vitest-fetch-mock": "^0.2.2"
  },
  "packageManager": "pnpm@8.15.4+sha1.c85a4305534f76d461407b59277b954bac97b5c4"
}
